# RNA-seq 分析工作流配置与使用方法

## 1. 工作流概述

该工作流共包含 15 个核心过程，依次完成以下任务：

| 步骤 | 过程 | 功能 |
|------|------|------|
| 1 | `STAR_GENOME_GENERATE` | 构建 STAR 索引 |
| 2 | `FASTQC_RAW` | 原始数据质量评估 |
| 3 | `FASTP_FILTER` | 数据过滤与修剪 |
| 4 | `FASTQC_CLEAN` | 过滤后数据质量评估 |
| 5 | `STAR_ALIGN` | 比对至参考基因组 |
| 6 | `FEATURECOUNTS` | 基因水平定量 |
| 7 | `CLEAN_AND_REORDER_COUNTS` | 清理并重排计数矩阵 |
| 8 | `QUANTIFICATION` | 计算 FPKM 与 TPM |
| 9 | `MULTIQC_ALL` | 汇总质控报告 |
| 10 | `FPKM_VIOLIN_PLOT` | 绘制表达量小提琴图 |
| 11 | `QC_AND_PCA` | 相关性热图与 PCA |
| 12 | `DIFFERENTIAL_EXPRESSION` | edgeR 差异表达分析 |
| 13 | `GO_ENRICHMENT` | GO 富集分析 |
| 14 | `KEGG_ENRICHMENT` | KEGG 富集分析 |
| 15 | `GSEA_ANALYSIS` | GSEA 分析 |

所有分析均运行在 **Singularity 容器** 中，保证了环境的可重复性。

---

## 2. 环境依赖

### 2.1 系统软件
- **Nextflow**（版本 ≥ 22.10.0）
- **Singularity**（版本 ≥ 3.8）或 **Docker**（若改用 Docker）

### 2.2 容器镜像
- 工作流使用预构建的 Singularity 镜像：`rna-seq.sif`（路径需在配置文件中指定）
- 镜像内应包含以下软件：
  - FastQC, Fastp, STAR, featureCounts, samtools
  - R 环境及所需包：edgeR, ggplot2, dplyr, ComplexHeatmap, clusterProfiler, GOplot, enrichplot, ggrepel, tidyr, RColorBrewer, AnnotationHub, showtext 等
  - Python 3 及 pandas, argparse 等模块

### 2.3 脚本文件
所有自定义脚本（`.py` 和 `.R`）需存放于服务器指定目录（如 `/home/data/t210528/scripts`），并在 Nextflow 中通过容器挂载可访问。

---

## 3. 数据准备

### 3.1 输入文件结构
```
项目根目录/
├── input_data/
│   ├── genotypic_data/               # FASTQ 文件目录
│   │   ├── sample1_1.fastq.gz
│   │   ├── sample1_2.fastq.gz
│   │   ├── sample2_1.fastq.gz
│   │   └── ...
│   └── phenotypic_data/
│       └── phenotypic_data.tsv       # 样本表型文件
├── references/
│   ├── GCF_034140825.1_ASM3414082v1_genomic.fna   # 参考基因组
│   └── GCF_034140825.1_ASM3414082v1_genomic.gtf   # 注释文件
├── singularity/
│   └── rna-seq.sif                    # Singularity 镜像
└── results/                           # 输出目录（自动创建）
```

### 3.2 表型文件格式（`phenotypic_data.tsv`）
制表符分隔，至少两列：
```
sample    group
Control1  Control
Control2  Control
Treatment1 Treatment
Treatment2 Treatment
```

- 第一列为样本名（必须与 FASTQ 文件名前缀完全一致）
- 第二列为分组信息（如 `Control` 和 `Treatment`）

### 3.3 FASTQ 文件命名规则
双端测序文件必须包含 `_1` 和 `_2` 后缀，例如：
```
SampleA_1.fastq.gz
SampleA_2.fastq.gz
```

---

## 4. 配置文件修改（`nextflow.config`）

工作流通过 `nextflow.config` 进行参数配置。用户需根据实际环境修改以下内容：

```nextflow
params {
    // --- 路径参数 ---
    base_dir      = "/path/to/your/project"          // 项目根目录
    genotypic_dir = "${base_dir}/input_data/genotypic_data"
    phenotype_dir = "${base_dir}/input_data/phenotypic_data/phenotypic_data.tsv"
    reference     = "${base_dir}/references/your_genome.fna"
    annotation    = "${base_dir}/references/your_annotation.gtf"
    output_dir    = "${base_dir}/results"
    star_index    = "${output_dir}/star_index"       // 索引输出位置
    sing_image    = "${base_dir}/singularity/rna-seq.sif"
    
    // --- 分析参数 ---
    species        = "Oryza_sativa"                  // 物种名称（用于富集分析）
    control_group  = "Control"                      // 对照组名称
    experimental_group = "Treatment"                // 实验组名称
    logFC          = 1                               // log2 倍数变化阈值
    FDR            = 0.05                            // 校正后 p 值阈值
}

// Singularity 配置
singularity {
    enabled     = true
    autoMounts  = true
    runOptions  = "-B /path/to/scripts"              // 挂载脚本目录（包含所有 .py/.R 脚本）
}

// 资源分配
process {
    executor    = 'local'            // 或 'slurm' 等集群调度器
    container   = params.sing_image
    cpus        = 8                  // 每个进程默认 CPU 数
    memory      = '32 GB'            // 每个进程默认内存
    maxForks    = 4                  // 最大并发进程数
}
```

### 注意事项
- **脚本目录挂载**：`runOptions` 中需添加 `-B /absolute/path/to/scripts`，确保容器内可访问 Python/R 脚本。
- **物种名称**：需与 `go_enrichment.R` 等脚本中的 `species` 条件分支匹配。支持的水稻物种名为 `"Oryza_sativa"`。
- **参考基因组**：建议使用与注释文件配套的版本，避免基因 ID 不匹配。

---

## 5. 运行工作流

### 5.1 启动命令
在项目根目录执行：
```bash
nextflow run main.nf -c nextflow.config -resume
```
- `main.nf`：主流程脚本文件名（请根据实际命名调整）
- `-c nextflow.config`：指定配置文件
- `-resume`：从上次中断处继续运行（推荐）

### 5.2 监控运行状态
Nextflow 会实时打印进程执行日志。可使用以下命令查看详细报告：
```bash
nextflow log
```

### 5.3 清理缓存
若需完全重新运行，可删除工作目录：
```bash
rm -rf work
```

---

## 6. 输出结果说明

所有结果默认保存在 `${base_dir}/results/` 目录下，结构如下：

```
results/
├── fastqc_raw/               # 原始数据质控报告
├── fastp/                    # 过滤后 FASTQ 及报告
├── fastqc_clean/             # 过滤后质控报告
├── star_aligned/             # 各样本 STAR 比对结果（BAM 文件及日志）
├── featurecounts/            # featureCounts 原始计数矩阵、清理后矩阵
├── quantification/           # FPKM 与 TPM 矩阵
├── visualization/            # 小提琴图、相关性热图、PCA 图
├── differential_expression/  # edgeR 结果（DEG 表格、火山图、热图）
├── go_enrichment/            # GO 富集结果（CSV 文件、条形图）
├── kegg_enrichment/          # KEGG 富集结果（CSV 文件、圈图）
├── gsea/                     # GSEA 结果（GO 与 KEGG 的 CSV 及图形）
├── star_index/               # STAR 索引（仅首次生成）
└── multiqc_reports/          # MultiQC 综合报告（HTML 文件）
```

---

## 7. 常见问题与注意事项

### 7.1 容器路径问题
- 若容器内无法找到 Python/R 脚本，请检查 `singularity.runOptions` 是否正确挂载了脚本目录，且脚本路径在进程内使用绝对路径或相对路径均可（如 `/home/data/t210528/scripts/xxx.R`）。

### 7.2 物种数据库
- 对于水稻（Oryza sativa），GO/KEGG 分析需要预先准备 SQLite 数据库文件（`org.Oryza_sativa_Japonica_Group.eg.sqlite`），脚本中指定了 `/opt/annotationhub/` 路径。若路径不符，请修改相应 R 脚本中的 `loadDb` 路径。

### 7.3 资源调整
- 若样本数较多或内存不足，可调整 `process.cpus`、`process.memory` 和 `maxForks` 值。

### 7.4 自定义阈值
- 差异表达分析的 FDR 和 logFC 阈值在 `nextflow.config` 中修改即可，无需改动 R 脚本。

### 7.5 链特异性文库
- 若您的数据是链特异性文库，需在 `STAR_ALIGN` 和 `FEATURECOUNTS` 过程中添加相应参数。修改 `main.nf` 中的 `STAR_ALIGN` 脚本块，增加 `--outSAMstrandField intronMotif`；修改 `FEATURECOUNTS` 脚本块，增加 `-s 1`（或 `-s 2`，根据文库类型）。

---

## 8. 获取帮助

若在配置或运行过程中遇到问题，请检查：
- Nextflow 日志（控制台输出）
- 各进程的 `.command.log` 文件（位于 `work/` 目录下）
- 确保所有依赖文件路径正确且具有读取权限

如需进一步调整分析流程或添加新步骤，可编辑 `main.nf` 脚本，并保持与现有过程一致的输入/输出通道连接。
